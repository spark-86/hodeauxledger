import { loadConfig } from "./configService.js";

export const Startup = {
    async init() {
        const config = loadConfig();

        // Get the tip of the root scope

        // If tips don't match (heh ;)) refresh our copy of thhe root
        // scope.

        // Process our local ledger first
    },
};
